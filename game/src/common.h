#pragma once

#include "math.h"


typedef struct route {
      int no_lengths;
      int grade;
} route_t;


typedef struct location {
      int NEVIM;
} location_t;

enum Places {Aljazev dom, Triglav, Blllaaa}
